// TEAM MEMBER home. One box in the middle: problem or idea, one line of text, send. The routing
// table proposes the desk it lands on and the date an answer is owed; nothing else on the page.
import { RaiseView } from "@/components/dashboard/team/RaiseView";
export const metadata = { title: "Raise" };
export default function RaisePage() {
  return <RaiseView />;
}

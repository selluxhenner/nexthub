# team

The member's three places on the simple shell: RaiseView (the box), DashboardView (every case as a
list), SentView (what happened to what I sent - one card per case with the four steps and the reply).
